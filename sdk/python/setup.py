from setuptools import setup, find_packages

setup(
    name="ai-gateway-client",
    version="0.1.0",
    description="Python SDK for AI Gateway",
    packages=find_packages(),
    python_requires=">=3.8",
)
